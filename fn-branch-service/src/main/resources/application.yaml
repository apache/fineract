server:
  port: 9005
spring:
  application:
    name: branch-service
  datasource:
    url: jdbc:postgresql://localhost:5432/fineract_branches
    username: postgres
    password: vakporize


  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        connection:provider_disables_autocommit: true