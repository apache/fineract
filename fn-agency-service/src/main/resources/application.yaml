server:
  port: 9002
spring:
  application:
    name: agency-service
  datasource:
    url: jdbc:postgresql://localhost:5432/fineract_agencies
    username: postgres
    password: vakporize
  mvc:
    pathMatch:
      matching-strategy: ant_path_matcher


  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        connection:provider_disables_autocommit: true