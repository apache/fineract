\loan\?\transactions\id get exception if loan doesn't match id.... should loan be taken out here?
template for waiver/repayment... is it same for both? no type needed? (might have got this wrong)
new loan template ... is it 2 templates... one to pick product... and 2nd to return default properties in selected product or is it meant that if only one product then use that. 

recreate()


Lost permissions again... restore fixed it up... must be some prob with permissions/roles maintenance
also, maybe I should be using a user that has everything but role admin.

users - delete still falls over if user doesn't exist. DOING

Loan Products
List/Retrieve - should show textual values of enums? DOING
Create - principal is later called principalMoney , also consistency between other loan fields and loan product fields with Type on them DOING

clients - parentid = id or id 1 has parentId not null DOING
list clients notes - noteType needs a 'text'
	createdOn & updatedOn values... do they need reformatting?
	are they ord

updating client note - ignores clientId anyhow! so is it really a notes resource? 

loan template
Formatted field still there
Type fields rename and with associated text?
allowedProduct should maybe just be id & name
which fields have defaults as not all on NewLoanWorkflowStepOneData dto seem to be default.


Additional Fields
put right message for parameter not recognized etc.(plus tht 9 other jpwwrongmsg items)



Identify Fields that need explaining
Clients
displayName, externalId, maxJoinedOnOffsetFromToday


