server:
  port: 9003
spring:
  application:
    name: asset-service
  datasource:
    url: jdbc:postgresql://localhost:5432/fineract_assets
    username: postgres
    password: vakporize


  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        connection:provider_disables_autocommit: true
#  main:
#    allow-bean-definition-overiding: true
