server:
  port: 9007
spring:
  application:
    name: wallet-service
  datasource:
    url: jdbc:postgresql://localhost:5432/fineract_wallets
    username: postgres
    password: vakporize
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest


  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        connection:provider_disables_autocommit: true

rabbit:
  exchange:
    name: audit.exchange
  queue:
    name: audit.accounting.queue
  routing:
    key: audit.accounting